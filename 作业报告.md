# RegexBlocks 课程设计作业报告

## 一、项目概述

### 项目名称
**RegexBlocks - 可视化正则表达式构建器**

### 项目简介
RegexBlocks 是一款创新的跨平台正则表达式可视化构建工具，采用类似 Scratch 的积木拖拽界面，使用户无需记忆复杂的正则表达式符号语法，即可直观地构建、编辑和测试正则表达式。项目基于 Qt 6 框架开发，支持 macOS、Windows 和 Linux 多个平台，适用于初学者学习正则表达式，也能满足开发者的日常需求。

### 核心亮点

#### 1. **Scratch 风格积木系统**
- 22 种语义化可视化积木，覆盖正则表达式的完整语法树
- 包括：字面量、字符类（`\d`, `\w`, `\s` 等）、量词（`?`, `*`, `+`, `{n,m}`）、字符集、锚点、分支逻辑、后向引用、分组等
- 支持物理嵌套的分组容器，可创建捕获组 `(...)` 和非捕获组 `(?:...)`

#### 2. **双向编辑与实时同步**
- **积木 → 正则**：拖拽积木时，右上角正则表达式以 60Hz 刷新速率实时更新
- **正则 → 积木**：直接在文本框输入正则表达式，按 Enter 自动解析为积木树并自动布局
- 完整的撤销/重做支持（Cmd+Z / Ctrl+Z）

#### 3. **高交互性设计**
- **智能拖拽**：量词自动吸附到可修饰项后方；拖动分组块时量词跟随移动
- **实时反馈**：蓝色鬼影预览、容器高亮效果、100ms 缓动滑入动画
- **多选操作**：Cmd/Ctrl + 单击进行多选，整体同步拖动
- **便利删除**：选中后按 Delete 删除，或拖出画布到红色区域

#### 4. **专业级测试工具**
- 支持 6 个正则选项：忽略大小写、全局、多行、Unicode、Sticky、DotAll
- 自动高亮测试文本中的所有匹配段落
- 捕获组列表一目了然，支持 `\0`、`\1`、`\2` 等后向引用替换

#### 5. **跨语言代码导出**
- 一键导出为 Python、JavaScript、Java、Rust、C++ Qt 等多种语言的代码片段
- 自动处理平台差异和标志转换

---

## 二、项目架构设计

### 2.1 总体架构

项目采用分层架构设计，分为四个主要模块：

```
┌─────────────────────────────────────────┐
│         UI 层（用户界面）                │
│   MainWindow | BlockCanvas              │
│   BlockPalette | TestPanel              │
└────────────┬────────────────────────────┘
             │
┌────────────▼────────────────────────────┐
│      Graphics 层（图形渲染）             │
│    BlockGraphicsItem                    │
│    (负责积木的视觉表现)                 │
└────────────┬────────────────────────────┘
             │
┌────────────▼────────────────────────────┐
│       Core 层（业务逻辑）                │
│   Block 类体系 | RegexCompiler           │
│   RegexParser | Commands                │
└────────────┬────────────────────────────┘
             │
┌────────────▼────────────────────────────┐
│       Util 层（工具函数）                │
│   AppIcon | Fonts | Theme               │
└─────────────────────────────────────────┘
```

### 2.2 Core 模块设计

**职责**：正则表达式的数据表示、编译、解析和操作命令

#### 2.2.1 Block 类体系（核心数据模型）

Block 是整个项目的数据基础，采用**多态 + 模板方法模式**，完整的类体系如下：

```cpp
// 抽象基类
class Block {
    virtual BlockType type() const = 0;
    virtual QString toRegex() const = 0;       // 关键方法：生成正则字符串
    virtual QString displayText() const = 0;   // 积木上的显示文字
    virtual QString category() const = 0;      // 分类（用于左侧库）
    virtual QColor color() const = 0;          // 语义色彩编码
    virtual std::unique_ptr<Block> clone() const = 0;
    
    // 序列化（JSON）
    QJsonObject toJson() const;
    static std::unique_ptr<Block> fromJson(const QJsonObject &json);
    static std::unique_ptr<Block> createDefault(BlockType type);
protected:
    virtual void writeJson(QJsonObject &out) const = 0;
    virtual void readJson(const QJsonObject &in) = 0;
};
```

**核心子类详解**：

1. **LiteralBlock（字面量）**
   ```cpp
   class LiteralBlock : public Block {
       QString m_value;  // 存储文本 "abc"、"hello" 等
       
       // 关键实现：转义规则
       QString toRegex() const {
           QString out;
           static const QString meta = "\\^$.|?*+()[]{}";
           for (QChar c : m_value) {
               if (meta.contains(c)) out.append('\\');  // 只转义真正的元字符
               out.append(c);
           }
           return out;  // 结果示例：a → "a"，. → "\\."
       }
   };
   ```
   **特色**：智能转义 - 只转义正则元字符，不像 `QRegularExpression::escape` 那样过度转义，保证输出可读且往返一致

2. **GroupBlock（分组容器）**
   ```cpp
   class GroupBlock : public Block {
       bool m_capturing = true;           // true: (...)，false: (?:...)
       QString m_name;                    // 命名组: (?<name>...) 或 (?P<name>...)
       std::vector<std::unique_ptr<Block>> m_children;  // 子积木，形成树结构
       
       QString toRegex() const {
           // 递归编译子积木
           QString inner = RegexCompiler::compile(m_children);
           if (m_capturing && m_name.isEmpty())
               return "(" + inner + ")";
           else if (m_capturing)
               return "(?<" + m_name + ">" + inner + ")";
           else
               return "(?:" + inner + ")";
       }
   };
   ```
   **特色**：
   - 树形嵌套：支持任意深度的分组嵌套，子积木可自由拖入/拖出
   - 命名分组：支持 `(?<name>...)` 和 `(?P<name>...)` 两种语法
   - 在 BlockGraphicsItem 中渲染为"容器"，视觉上包含子积木

3. **QuantifierBlock（量词）**
   ```cpp
   class QuantifierBlock : public Block {
       int m_min, m_max;        // min=2, max=4 → {2,4}；max=-1 → 无上限
       bool m_lazy = false;     // true: *?、+? 等
       bool m_linkedToPrevForDrag = true;  // UX: 拖动前项时自动跟随
       
       QString toRegex() const {
           if (m_min == 0 && m_max == 1) return m_lazy ? "??" : "?";
           if (m_min == 0 && m_max == -1) return m_lazy ? "*?" : "*";
           if (m_min == 1 && m_max == -1) return m_lazy ? "+?" : "+";
           // 其他: {n}, {n,}, {n,m} 等
           QString result;
           if (m_max == -1)
               result = QString("{%1,}").arg(m_min);
           else if (m_min == m_max)
               result = QString("{%1}").arg(m_min);
           else
               result = QString("{%1,%2}").arg(m_min, m_max);
           return m_lazy ? result + "?" : result;
       }
   };
   ```
   **特色**：
   - Lazy 标志：支持非贪婪量词 `*?`、`+?` 等
   - 智能简化：`{0,1}` 显示为 `?`，自动生成最简正则
   - 跟随拖动：`m_linkedToPrevForDrag` 使量词能够自动跟随主块移动

4. **其他重要子类**：
   - **CharClassBlock**：`\d`、`\w`、`\s` 等预定义字符类，含中英文标签
   - **CharSetBlock**：`[abc]`、`[a-z]`、`[^...]` 等字符集，支持反向
   - **AnchorBlock**：`^`、`$`、`\b`、`\B` 等锚点
   - **AlternationBlock**：`|` 分支逻辑
   - **BackReferenceBlock**：`\1`、`\k<name>` 后向引用

**设计模式优势**：
- ✅ **多态**：统一接口，易于扩展新积木类型
- ✅ **模板方法**：每个子类只需实现 `toRegex()` 和序列化方法
- ✅ **工厂模式**：`createDefault()` 和 `fromJson()` 统一创建积木
- ✅ **JSON 序列化**：完整的往返支持，保证项目可持久化

#### 2.2.2 RegexCompiler（编译器 - 高效的字符串生成）

RegexCompiler 的核心任务是将 Block 序列转换为正则表达式字符串，同时进行合法性检查。

```cpp
class RegexCompiler {
    struct Result {
        QString regex;      // 生成的正则字符串
        bool valid;         // 是否合法
        QString warning;    // 警告信息
    };
    
    static Result compile(const QList<Block*> &blocks);
};
```

**编译算法详解**：

```cpp
// 核心实现（pseudocode）
QString compileSequence(const QList<Block*> &blocks, RegexCompiler::Result &r) {
    QString out;
    out.reserve(blocks.size() * 4);  // 预分配内存，避免重复分配
    
    Block *prev = nullptr;
    for (int i = 0; i < blocks.size(); ++i) {
        Block *b = blocks[i];
        
        // 量词合法性检查
        if (b->type() == BlockType::Quantifier) {
            if (!prev) {  // 量词在最前
                r.valid = false;
                r.warning = "量词不能放在最前面";
                continue;
            }
            if (prev->type() == BlockType::Quantifier) {  // 量词叠加
                r.valid = false;
                r.warning = "量词不能修饰量词";
                continue;
            }
        }
        
        // GroupBlock 递归编译
        if (b->type() == BlockType::Group) {
            auto *g = static_cast<GroupBlock*>(b);
            QList<Block*> kids;
            for (const auto &c : g->children()) kids.append(c.get());
            QString inner = compileSequence(kids, r);  // 递归
            
            if (g->capturing() && g->name().isEmpty())
                out += "(" + inner + ")";
            else if (g->capturing())
                out += "(?<" + g->name() + ">" + inner + ")";
            else
                out += "(?:" + inner + ")";
        } else {
            // 直接调用 Block 的 toRegex()
            out += b->toRegex();
        }
        prev = b;
    }
    return out;
}
```

**关键特点**：
1. **递归设计**：自然处理嵌套 GroupBlock，每层递归都进行独立的合法性检查
2. **预分配优化**：使用 `out.reserve()` 减少字符串重新分配的开销
3. **实时合法性检查**：检测量词位置错误、量词叠加等常见错误
4. **暂停执行**：发现错误后继续编译其他部分，收集完整的警告信息

#### 2.2.3 RegexParser（解析器 - 正则字符串反向解析）

RegexParser 实现了将用户输入的正则表达式字符串反向解析为 Block 树的功能，这是项目的"正 → 反"双向流程。

```cpp
class RegexParser {
    struct Result {
        bool ok = false;
        std::vector<std::unique_ptr<Block>> blocks;  // 解析后的 Block 树
        QString error;      // 错误信息
        int errorPos = -1;  // 错误位置
    };
    
    static Result parse(const QString &regex);
};
```

**解析流程（递归下降）**：
```
parse()
  → parseAlternation()      // 处理 | (最低优先级)
    → parseGroup()           // 处理分组 (...)
      → parseSequence()      // 处理积木序列
        → parseQuantifier()  // 处理量词 ? * + {n,m}
          → parseAtom()      // 处理基础项：字面量、字符类等
```

**关键优化**：
1. **往返一致性**：确保 `parse(compile(blocks)) == blocks`
2. **错误位置报告**：记录错误发生的具体位置，便于用户修正
3. **增量解析**：当用户逐字输入时，只需重新解析改变部分

#### 2.2.4 Commands 系统（撤销/重做的命令模式）

Commands 系统为所有用户操作提供原子化处理和撤销/重做支持，充分利用 Qt 的 QUndoStack 机制。

```cpp
// 四个主要命令类
class AddBlockCommand : public QUndoCommand {
    BlockCanvas *m_canvas;
    QJsonObject m_json;            // 序列化的 Block 数据
    BlockCanvas::BlockPath m_parentPath;  // 目标父路径
    int m_childIdx;                // 插入位置
    
    void redo() {
        auto block = Block::fromJson(m_json);
        m_canvas->rawInsert(std::move(block), m_parentPath, m_childIdx);
    }
    void undo() {
        m_canvas->rawRemoveAt(BlockCanvas::BlockPath(m_parentPath) << m_childIdx);
    }
};

class RemoveBlockCommand : public QUndoCommand {
    QJsonObject m_json;      // 删除前的快照（含嵌套子树）
    BlockCanvas::BlockPath m_path;
    
    void redo() {
        m_json = m_canvas->blockAt(m_path)->toJson();  // 保存快照
        m_canvas->rawRemoveAt(m_path);
    }
    void undo() {
        auto block = Block::fromJson(m_json);
        // 按原路径还原
        m_canvas->rawInsert(std::move(block), parent_path, idx);
    }
};

class MoveBlockCommand : public QUndoCommand {
    BlockCanvas::BlockPath m_fromPath, m_toParent;
    int m_toChildIdx;
    bool m_firstRun = true;  // 首次 redo 是 no-op（拖动已完成）
    
    void redo() {
        if (!m_firstRun) {  // 后续 redo
            m_canvas->rawMoveAcross(m_fromPath, m_toParent, m_toChildIdx);
        }
        m_firstRun = false;
    }
    void undo() {
        // 反向搬移
        m_canvas->rawMoveAcross(m_toParent << m_toChildIdx, m_fromPath.dropLast());
    }
};

class EditBlockCommand : public QUndoCommand {
    QJsonObject m_oldJson, m_newJson;
    BlockCanvas::BlockPath m_path;
    bool m_firstRun = true;
    
    void redo() {
        if (!m_firstRun)
            m_canvas->rawReplaceAt(m_path, Block::fromJson(m_newJson));
        m_firstRun = false;
    }
    void undo() {
        m_canvas->rawReplaceAt(m_path, Block::fromJson(m_oldJson));
    }
};
```

**设计巧妙之处**：
1. **JSON 快照**：每个命令都保存操作前的 JSON 快照，完整还原嵌套数据
2. **原子操作**：`raw*` 系列方法实现原子操作，仅供命令调用
3. **首次 no-op**：MoveBlockCommand 和 EditBlockCommand 的首次 redo 是 no-op，因为拖动/编辑已发生，避免重复操作
4. **完整撤销历史**：支持 Cmd+Z 和 Cmd+Shift+Z 的完整撤销/重做流程

---

### 2.2.5 BlockPath 寻址算法（嵌套积木的精确定位）

BlockPath 是一个 `QList<int>` 类型的列表，用于精确定位嵌套积木：

```cpp
using BlockPath = QList<int>;

// 示例：
BlockPath p1 = {};           // 空列表 = 顶层（父路径）
BlockPath p2 = {3};          // 顶层第 3 个积木
BlockPath p3 = {2, 0};       // 顶层第 2 个 GroupBlock 的第 0 个子积木
BlockPath p4 = {1, 2, 1};    // 顶层第 1 个 GroupBlock 的第 2 个分组的第 1 个子积木
```

**寻址算法**：

```cpp
Block *BlockCanvas::blockAt(const BlockPath &path) const {
    if (path.isEmpty()) return nullptr;  // 顶层不是一个 Block
    
    Block *current = nullptr;
    
    // 第 0 层：顶层
    if (path[0] < m_items.size()) {
        current = m_items[path[0]]->block();
    }
    
    // 逐层向下
    for (int i = 1; i < path.size(); ++i) {
        if (!current || current->type() != BlockType::Group) {
            return nullptr;  // 路径无效
        }
        auto *group = static_cast<GroupBlock*>(current);
        if (path[i] < static_cast<int>(group->children().size())) {
            current = group->children()[path[i]].get();
        } else {
            return nullptr;
        }
    }
    return current;
}
```

**时间复杂度**：O(d)，其中 d 是路径深度（通常很小）

---

### 2.3 UI 模块设计

**职责**：用户界面布局、交互响应、事件处理

#### 2.3.1 BlockCanvas（绘制区 - 核心交互引擎）

BlockCanvas 是项目的中心，整合了积木渲染、拖拽交互、编辑管理、撤销栈等核心功能。

```cpp
class BlockCanvas : public QGraphicsView {
    // === 核心数据结构 ===
    QGraphicsScene *m_scene;              // Qt 图形场景
    QList<BlockGraphicsItem*> m_items;    // 顶层积木的图形项
    QUndoStack *m_undoStack;              // 撤销/重做栈（容量 100）
    
    // === 公共 API（均走 undo stack）===
    // 追加积木到顶层（自动计算位置）
    void appendBlock(std::unique_ptr<Block> block);
    
    // 按 scene 坐标插入：支持嵌套插入（命中到哪层分组就插到哪层）
    void insertBlockAtScenePos(std::unique_ptr<Block> block, 
                               const QPointF &scenePos);
    
    // 删除 path 处的积木（含嵌套子树）
    void deleteBlockAt(const BlockPath &path);
    
    // 替换 path 处的积木（用于编辑参数）
    void replaceBlockAt(const BlockPath &path,
                        const QJsonObject &oldJson,
                        const QJsonObject &newJson);
    
    // 清空所有积木
    void clearBlocks();
    
    // === 原子操作（commands.cpp 调用，不入栈）===
    void rawInsert(std::unique_ptr<Block> block,
                   const BlockPath &parentPath, int childIdx);
    std::unique_ptr<Block> rawRemoveAt(const BlockPath &path);
    void rawMoveAcross(const BlockPath &fromPath,
                       const BlockPath &toParent, int toChildIdx);
    void rawReplaceAt(const BlockPath &path, std::unique_ptr<Block> newBlock);
    
    // === 路径寻址与查询 ===
    Block *blockAt(const BlockPath &path) const;
    BlockGraphicsItem *itemAt(const BlockPath &path) const;
    BlockPath pathOf(BlockGraphicsItem *item) const;
    
    // === 序列化 ===
    QJsonArray toJson() const;
    void loadJson(const QJsonArray &arr);
    
signals:
    void blocksChanged();      // 积木变化 → 重新编译、刷新正则、入 undo
    void blocksPreviewing();   // 拖动期间 → 仅刷新预览，不跑匹配
};
```

**关键设计**：

1. **三层 API 设计**：
   - **公共 API**（`appendBlock`、`insertBlockAtScenePos` 等）：走 QUndoStack，自动支持撤销
   - **原子操作**（`rawInsert`、`rawMove` 等）：实现原子修改，供命令类调用
   - **辅助方法**（`blockAt`、`pathOf` 等）：查询接口

2. **拖拽交互流程**：
   ```cpp
   dragEnterEvent()  → 检查 MIME 类型（application/x-regex-block）
   dragMoveEvent()   → 实时预览：高亮目标容器
   dropEvent()       → 调用 insertBlockAtScenePos，自动入 undo stack
   ```

3. **嵌套容器支持**：
   ```cpp
   // dropEvent 的核心逻辑
   QPointF dropPos = mapToScene(event->pos());
   BlockGraphicsItem *targetItem = itemAt(dropPos);  // 命中判定
   
   if (targetItem && targetItem->isGroup()) {
       // 拖入 GroupBlock
       insertBlockAtScenePos(newBlock, dropPos);  // 自动插入到目标分组
   } else {
       // 拖入顶层
       insertBlockAtScenePos(newBlock, dropPos);
   }
   ```

4. **高频优化**：
   ```cpp
   // 拖动期间（BlockGraphicsItem 的 mouseMoveEvent）
   emit blocksPreviewing();  // 仅刷新 UI，不修改数据
   
   // 拖动结束（mouseReleaseEvent）
   emit blocksChanged();  // 完整流程：入 undo、编译、匹配
   ```

#### 2.3.2 BlockPalette（积木库 - 拖拽源管理）

BlockPalette 是左侧积木库，提供 22 种积木的可视化列表和拖拽源。

```cpp
class BlockPalette : public QListWidget {
    static const char *kMimeType;  // "application/x-regex-block"
    
    explicit BlockPalette(QWidget *parent = nullptr);
    
    // === 初始化 ===
    void populate();               // 按分类填充积木列表
    void addCategoryHeader(const QString &title);  // 分组标题（灰色、不可拖）
    void addBlockItem(std::unique_ptr<Block> prototype);  // 可拖拽项
    
    // === 主题支持 ===
    void applyThemedStyle();       // 主题切换时更新 QSS 样式
    
    // === 拖拽实现 ===
    void startDrag(Qt::DropActions supportedActions) override;
};
```

**populate() 的分类组织**：
```
字面量
  ├─ a-z（可编辑）
  ├─ 0-9（可编辑）
  └─ 特殊符号（可编辑）

字符类
  ├─ \d（数字）
  ├─ \w（字母数字下划线）
  ├─ \s（空白）
  ├─ .（任意字符）
  └─ 否定版本（\D, \W, \S）

量词
  ├─ ?（0 或 1）
  ├─ *（0 个或更多）
  ├─ +（1 个或更多）
  ├─ {n,m}（自定义范围，可编辑）
  └─ ...

分组与其他
  ├─ (...)（捕获分组）
  ├─ (?:...)（非捕获分组）
  ├─ |（或）
  └─ ...
```

**startDrag() 的 MIME 编码**：
```cpp
void BlockPalette::startDrag(Qt::DropActions supportedActions) {
    auto *item = currentItem();
    if (!item) return;
    
    // 获取该项的 Block 原型并克隆
    Block *proto = qvariant_cast<Block*>(item->data(Qt::UserRole));
    auto block = proto->clone();  // 重要：克隆，而非移动所有权
    
    // 序列化为 JSON
    QJsonObject json = block->toJson();
    QJsonDocument doc(json);
    QByteArray data = doc.toJson(QJsonDocument::Compact);
    
    // 创建 MIME 数据
    auto *mimeData = new QMimeData();
    mimeData->setData(kMimeType, data);
    
    // 启动拖拽
    auto *drag = new QDrag(this);
    drag->setMimeData(mimeData);
    drag->setPixmap(renderBlockAsPixmap(block.get()));  // 拖拽预览图
    drag->exec(supportedActions);
}
```

#### 2.3.3 TestPanel（测试面板 - 高频优化设计）

TestPanel 实现了实时正则匹配、高亮、替换等功能，其中最精妙的是**防抖 + 双路径**的高频优化设计。

```cpp
class TestPanel : public QWidget {
    Q_OBJECT
public:
    // === 数据接口 ===
    QString testText() const;
    void setTestText(const QString &text);
    
    QStringList flags() const;        // i/g/m/u/y/s
    void setFlags(const QStringList &flags);
    
    QString replacement() const;
    void setReplacement(const QString &s);
    
    // === 关键的双路径 API ===
    // 路径 1：完整匹配（拖动结束时调用）
    void setRegex(const QString &regex, bool valid = true, 
                  const QString &warning = {});
    
    // 路径 2：仅刷新预览（拖动期间高频调用，60Hz）
    void setRegexPreview(const QString &regex);
    
private slots:
    // 防抖处理
    void scheduleMatch();      // 延迟 100ms 后执行匹配
    void runMatch();           // 实际匹配逻辑
    
    // 用户交互
    void onMatchItemClicked(QListWidgetItem *item);
    void onReplaceAllClicked();
    
private:
    QLineEdit *m_regexDisplay;        // 显示当前正则
    QPlainTextEdit *m_testText;       // 测试文本输入
    QListWidget *m_matchList;         // 匹配结果列表
    QLabel *m_statusLabel;            // 状态（错误/警告）
    QLabel *m_matchCountLbl;          // 匹配数计数
    QTimer *m_debounce;               // 防抖定时器
};
```

**高频优化：防抖 + 双路径**：

```cpp
// 场景：用户拖动积木块，BlockCanvas 每 16ms（60Hz）触发一次 blocksPreviewing()

// ========== 路径 1：拖动期间（高频调用）==========
void TestPanel::setRegexPreview(const QString &regex) {
    // 仅更新文本显示，不执行正则匹配
    m_regexDisplay->setText(regex);
    // 成本：O(1) 字符串赋值
}

// ========== 路径 2：拖动结束（低频调用）==========
void TestPanel::setRegex(const QString &regex, bool valid, const QString &warning) {
    m_regexDisplay->setText(regex);
    
    // 评估有效性
    if (!valid) {
        m_statusLabel->setText("❌ " + warning);
        m_statusLabel->setStyleSheet("color: red;");
        return;
    }
    
    // 如果有效，延迟 100ms 再执行昂贵的匹配操作
    m_debounce->stop();
    m_debounce->start(100);  // 100ms 防抖
}

void TestPanel::scheduleMatch() {
    m_debounce->stop();
    QTimer::singleShot(100, this, &TestPanel::runMatch);
}

void TestPanel::runMatch() {
    QString text = m_testText->toPlainText();
    QString regex = m_regexDisplay->text();
    QStringList flags = this->flags();
    
    // 编译正则（含 flags）
    QRegularExpression::PatternOptions opts;
    if (flags.contains("i")) opts |= QRegularExpression::CaseInsensitiveOption;
    if (flags.contains("m")) opts |= QRegularExpression::MultilineOption;
    if (flags.contains("s")) opts |= QRegularExpression::DotMatchesEverythingOption;
    // ... 其他 flags
    
    QRegularExpression re(regex, opts);
    
    // 高亮匹配（使用 QPlainTextEdit::ExtraSelection）
    QList<QTextEdit::ExtraSelection> selections;
    auto iter = re.globalMatch(text);
    int matchCount = 0;
    
    while (iter.hasNext()) {
        auto match = iter.next();
        QTextEdit::ExtraSelection sel;
        sel.format.setBackground(QColor("#FFD700"));  // 黄色高亮
        sel.cursor = QTextCursor(m_testText->document());
        sel.cursor.setPosition(match.capturedStart());
        sel.cursor.setPosition(match.capturedEnd(), QTextCursor::KeepAnchor);
        selections.append(sel);
        matchCount++;
    }
    
    m_testText->setExtraSelections(selections);
    
    // 更新匹配列表
    m_matchList->clear();
    iter = re.globalMatch(text);
    while (iter.hasNext()) {
        auto match = iter.next();
        QString displayText = QString("Match: \"%1\"")
                            .arg(match.captured(0).left(50));
        auto *item = new QListWidgetItem(displayText);
        item->setData(Qt::UserRole, match.capturedStart());  // 保存位置
        
        // 列出捕获组
        for (int i = 1; i <= match.lastCapturedIndex(); ++i) {
            QString groupText = QString("  \\%1 = \"%2\"")
                             .arg(i).arg(match.captured(i));
            // ...
        }
        m_matchList->addItem(item);
    }
    
    m_matchCountLbl->setText(QString("共 %1 个匹配").arg(matchCount));
}
```

#### 2.3.4 MainWindow（主窗口 - 应用框架）

```cpp
class MainWindow : public QMainWindow {
    // === UI 组件 ===
    QSplitter *m_splitter;      // 三分栏：Palette | Canvas | TestPanel
    BlockPalette *m_palette;
    BlockCanvas *m_canvas;
    TestPanel *m_testPanel;
    
    // === 文件管理 ===
    QString m_currentFilePath;  // 当前打开文件路径
    bool m_dirty = false;       // 未保存修改标记
    
    QMenu *m_recentMenu;        // 最近文件菜单
    QStringList m_recentFiles;  // 最近 5 个文件
    
    // === 槽函数 ===
    void onNewRecipe();         // 新建
    void onOpenRecipe();        // 打开（文件对话框）
    void onSaveRecipe();        // 保存
    bool saveToPath(const QString &path);
    void loadRecipeFile(const QString &path);
    
    void loadTemplate(int idx);  // 加载预置模板（7 个）
    void loadDefaultExample();    // 启动时加载示例
    
    void onRegexImported(const QString &regex);  // 正则导入（反向解析）
    void showDialectInfo();      // 帮助对话框
    
    // === 窗口状态管理 ===
    void setDirty(bool dirty);
    void updateTitle();
    bool maybeSave();            // 提示保存
    void loadSettings();         // 加载窗口状态（大小、分栏宽度）
    void saveSettings();         // 保存窗口状态
    
    // === 文件格式 ===
    // .regexblocks 是 JSON 格式：
    // {
    //   "version": "1.0",
    //   "blocks": [...],     // QJsonArray，来自 BlockCanvas::toJson()
    //   "testText": "...",   // 测试文本
    //   "flags": ["i", "g"], // 正则选项
    //   "replacement": "..."  // 替换字符串
    // }
};
```

### 2.4 Graphics 模块设计

**职责**：积木的视觉表现、布局算法、拖拽反馈、动画效果

#### 2.4.1 BlockGraphicsItem（积木图形项 - 复杂的渲染与交互）

BlockGraphicsItem 是每个 Block 的视觉表现，需要处理绘制、几何计算、交互响应、动画等。

```cpp
class BlockGraphicsItem : public QGraphicsObject {
    Q_OBJECT
    
    // === 常量定义 ===
    static constexpr qreal kHeight = 56.0;       // 积木高度
    static constexpr qreal kPaddingX = 16.0;     // 左右内边距
    static constexpr qreal kRadius = 10.0;       // 圆角半径
    static constexpr qreal kSpacing = 8.0;       // 积木间距
    static constexpr qreal kBracketW = 22.0;     // GroupBlock 括号区宽度
    static constexpr qreal kInnerPad = 6.0;      // GroupBlock 内边距
    static constexpr qreal kEmptySlotW = 90.0;   // 空 GroupBlock 占位宽度
    
    // === 数据 ===
    std::unique_ptr<Block> m_block;              // 关联的 Block 数据
    QList<BlockGraphicsItem*> m_groupChildren;   // 子积木列表（仅 GroupBlock）
    
    // === 状态 ===
    bool m_dragging = false;                     // 正在拖拽
    bool m_pendingRemove = false;                // 将被删除
    bool m_selected = false;                     // 被选中
    bool m_slotHighlighted = false;              // 容器高亮（drop hover）
    bool m_attachHighlighted = false;            // 吸附高亮（量词）
    
    QPropertyAnimation *m_slideInAnim = nullptr; // 滑入动画
    qreal m_slideInProgress = 1.0;               // 滑入进度（0～1）
};
```

**关键设计 1：几何计算与布局**

```cpp
QRectF BlockGraphicsItem::boundingRect() const {
    if (!m_block) return QRectF();
    
    // 基础尺寸
    qreal w = 2 * kPaddingX;  // 左右内边距
    qreal h = kHeight;
    
    // 普通积木：宽度 = 内边距 + 文字宽度
    if (!isGroup()) {
        QFontMetricsF fm(displayFont());
        w += fm.horizontalAdvance(m_block->displayText());
    } else {
        // GroupBlock：[内容 | 右括号]
        // 如果有子积木，宽度 = 括号宽 + 内边距 + 子积木总宽 + 间距 + 括号宽
        // 如果为空，宽度 = 括号宽 + 占位符 + 括号宽
        if (m_groupChildren.isEmpty()) {
            w = kBracketW + kEmptySlotW + kBracketW;
        } else {
            w = kBracketW + kInnerPad;
            for (const auto *child : m_groupChildren) {
                w += child->boundingRect().width() + kSpacing;
            }
            w += kBracketW;
        }
    }
    
    return QRectF(0, 0, w, h);
}

void BlockGraphicsItem::layoutChildren() {
    if (!isGroup()) return;
    
    // 子积木的排列：[左括号 | 内边距 | 子1 | 间距 | 子2 | ... | 右括号]
    qreal x = kBracketW + kInnerPad;
    for (auto *child : m_groupChildren) {
        child->setPos(x, 0);
        x += child->boundingRect().width() + kSpacing;
    }
    
    update();  // 触发重绘
}
```

**关键设计 2：绘制逻辑**

```cpp
void BlockGraphicsItem::paint(QPainter *painter, 
                              const QStyleOptionGraphicsItem *option,
                              QWidget *widget) {
    const QRectF rect = boundingRect();
    const QColor blockColor = m_block->color();
    
    // === 背景填充 ===
    QPainterPath path;
    path.addRoundedRect(rect, kRadius, kRadius);
    
    if (m_pendingRemove) {
        // 待删除：红色透明
        painter->fillPath(path, QColor("#FF5252").lighter(120));
    } else if (m_selected) {
        // 已选中：颜色加深
        painter->fillPath(path, blockColor.lighter(80));
    } else {
        // 正常：语义色彩
        painter->fillPath(path, blockColor);
    }
    
    // === 边框 ===
    if (m_slotHighlighted) {
        // 容器高亮：粗蓝色边框
        painter->setPen(QPen(QColor("#2196F3"), 3));
    } else if (m_attachHighlighted) {
        // 吸附高亮：黄色虚线
        painter->setPen(QPen(QColor("#FFC107"), 2, Qt::DashLine));
    } else {
        // 普通：深色边框
        painter->setPen(QPen(blockColor.darker(150), 1));
    }
    painter->drawPath(path);
    
    // === 文字 ===
    if (!isGroup()) {
        painter->setPen(Qt::white);
        painter->setFont(displayFont());
        painter->drawText(rect, Qt::AlignCenter, m_block->displayText());
    } else {
        // GroupBlock：绘制左右括号
        QColor bracketColor = blockColor.darker(120);
        painter->setPen(bracketColor);
        painter->setFont(displayFont().bold());
        
        // 左括号
        painter->drawText(QRectF(0, 0, kBracketW, kHeight), 
                         Qt::AlignCenter, "[");
        
        // 右括号
        painter->drawText(QRectF(rect.width() - kBracketW, 0, 
                                kBracketW, kHeight),
                         Qt::AlignCenter, "]");
        
        // 如果为空，显示占位文本
        if (m_groupChildren.isEmpty()) {
            painter->setPen(QColor(128, 128, 128, 128));
            painter->drawText(rect, Qt::AlignCenter, "拖入积木");
        }
    }
}
```

**关键设计 3：拖拽交互**

```cpp
void BlockGraphicsItem::mousePressEvent(QGraphicsSceneMouseEvent *event) {
    if (event->button() != Qt::LeftButton) return;
    
    // 多选：Cmd/Ctrl + 单击
    if (!(event->modifiers() & Qt::ControlModifier)) {
        scene()->clearSelection();
    }
    
    setSelected(true);
    m_dragging = true;
    m_dragStartPos = event->scenePos();
    emit dragStarted(this);
}

void BlockGraphicsItem::mouseMoveEvent(QGraphicsSceneMouseEvent *event) {
    if (!m_dragging) return;
    
    QPointF delta = event->scenePos() - m_dragStartPos;
    
    // 同步移动所有选中项
    for (auto *item : scene()->selectedItems()) {
        if (auto *blockItem = qgraphicsitem_cast<BlockGraphicsItem*>(item)) {
            blockItem->moveBy(delta.x(), delta.y());
            emit dragMovedBy(blockItem, delta);
        }
    }
    
    m_dragStartPos = event->scenePos();
    
    // 检查是否在"删除区"
    if (event->scenePos().y() > kTrashZoneTop) {
        setPendingRemove(true);
    } else {
        setPendingRemove(false);
    }
}

void BlockGraphicsItem::mouseReleaseEvent(QGraphicsSceneMouseEvent *event) {
    if (!m_dragging) return;
    
    m_dragging = false;
    bool willRemove = m_pendingRemove;
    setPendingRemove(false);
    
    emit dragEnded(this, willRemove);
}

void BlockGraphicsItem::mouseDoubleClickEvent(QGraphicsSceneMouseEvent *event) {
    // 双击请求编辑参数
    emit requestEdit(this);
}
```

**关键设计 4：GroupBlock 容器的 drop 处理**

```cpp
void BlockGraphicsItem::dropEvent(QGraphicsSceneDragDropEvent *event) {
    if (!isGroup()) return;
    
    setSlotHighlighted(false);
    
    // 反序列化 MIME 数据
    QByteArray data = event->mimeData()->data(BlockPalette::kMimeType);
    QJsonDocument doc = QJsonDocument::fromJson(data);
    QJsonObject json = doc.object();
    
    auto newBlock = Block::fromJson(json);
    if (!newBlock) return;
    
    // 计算插入位置（在子积木之间）
    QPointF localPos = event->pos();
    qreal x = kBracketW + kInnerPad;
    int insertIdx = 0;
    
    for (int i = 0; i < m_groupChildren.size(); ++i) {
        auto *child = m_groupChildren[i];
        if (localPos.x() < x + child->boundingRect().width() / 2) {
            insertIdx = i;
            break;
        }
        x += child->boundingRect().width() + kSpacing;
        insertIdx = i + 1;
    }
    
    // 发出插入事件，由 BlockCanvas 处理
    event->acceptProposedAction();
    emit blockDropped(this, insertIdx, std::move(newBlock));
}
```

**关键设计 5：动画效果**

```cpp
void BlockGraphicsItem::playSlideInAnimation() {
    // 新增积木的滑入动画：从顶部滑入，同时透明度从 0 变 1
    m_slideInProgress = 0.0;
    
    m_slideInAnim = new QPropertyAnimation(this, "slideInProgress");
    m_slideInAnim->setDuration(kSlideDurationMs);  // 100ms
    m_slideInAnim->setStartValue(0.0);
    m_slideInAnim->setEndValue(1.0);
    m_slideInAnim->setEasingCurve(QEasingCurve::OutCubic);
    
    connect(m_slideInAnim, &QPropertyAnimation::finished, this, [this]() {
        m_slideInProgress = 1.0;
        update();
    });
    
    m_slideInAnim->start();
}
```

**GroupBlock 的视觉特点**：
- **左括号 `[`** 和 **右括号 `]`**：视觉上强调这是一个容器
- **子积木物理嵌入**：子积木的 `setParentItem(this)` 使其在几何变换时跟随父项
- **动态宽度**：子积木增加/减少时，容器自动调整宽度
- **drop 预览**：拖入新积木时，容器高亮，提示可插入

---

## 三、技术特点与实现亮点

### 3.1 数据与 UI 分离

- **Block 类**：纯数据模型，不包含任何 GUI 逻辑，可独立序列化和复用
- **BlockGraphicsItem**：渲染视图，负责绘制和交互
- 优势：易于测试、扩展和维护

### 3.2 命令模式 & 撤销/重做

- 所有修改操作通过 `QUndoCommand` 封装
- 自动入栈到 `BlockCanvas::m_undoStack`
- 用户可无限撤销/重做（不消耗额外内存）

### 3.3 高频优化

- 拖动期间：仅刷新 UI，不执行正则匹配（`setRegexPreview`）
- 停止拖动后：执行完整匹配（`setRegex` + `runMatch`）
- 防抖处理：100ms 延迟，避免快速输入时的重复计算

### 3.4 嵌套分组支持

- BlockPath 寻址：`[]` 表示顶层，`[2, 0]` 表示第 2 个分组的第 0 个子块
- 递归编译：GroupBlock 自动递归编译子积木
- 拖拽精确度：支持拖入/拖出分组容器

### 3.5 跨平台兼容性

- 使用 Qt 6 框架，原生支持 macOS、Windows、Linux
- CMake + Ninja 构建系统，配置统一
- 正则引擎：Qt 的 `QRegularExpression`（PCRE2 兼容）

---

## 四、小组成员分工

#### 都瑞泽 - 核心逻辑

**负责模块**：Core 模块 + Commands（命令系统）

**具体职责**：
1. **Block 类体系设计与实现**
   - 抽象基类 Block 接口定义
   - 8+ 个具体 Block 子类的实现（LiteralBlock、CharClassBlock、QuantifierBlock 等）
   - 子类间的关系和交互逻辑
   - JSON 序列化/反序列化机制

2. **RegexCompiler 正则编译器**
   - 遍历 Block 序列，生成正则字符串
   - 递归处理嵌套 GroupBlock
   - 合法性检查和警告生成
   - 边界情况处理（如量词在最前面）

3. **RegexParser 正则解析器**
   - 词法分析：识别转义序列、元字符、字面量
   - 语法分析：递归下降解析分组、分支、量词等
   - 构建 Block 树
   - 错误处理和提示

4. **Commands 命令系统**
   - 设计各类 QUndoCommand 子类（InsertBlockCommand、DeleteBlockCommand 等）
   - 实现原子操作 `rawInsert`、`rawRemove`、`rawMove` 等
   - 与 BlockCanvas 的接口集成
   - 撤销/重做的完整流程

5. **单元测试**
   - Block 的序列化/反序列化测试
   - RegexCompiler 的编译正确性测试
   - RegexParser 的解析准确性测试


---

#### 卢怡霏 - UI 与交互

**负责模块**：UI 模块 + Graphics 模块 + Theme 主题系统

**具体职责**：
1. **MainWindow 主窗口**
   - 菜单栏设计与实现（文件、编辑、帮助菜单）
   - 三分栏布局（左：积木库，中：绘制区，右：测试面板）
   - 文件管理：新建、打开、保存、最近文件列表
   - 模板系统：加载 7 个预置模板
   - 窗口状态持久化

2. **BlockCanvas 绘制区**
   - QGraphicsScene/QGraphicsView 的集成
   - Block 树与 BlockGraphicsItem 的对应关系维护
   - 拖拽交互的完整流程：dragEnter → dragMove → dragLeave → drop
   - BlockPath 寻址算法（支持嵌套查询）
   - 删除、多选等快捷键操作

3. **Graphics 图形渲染**（与 BlockGraphicsItem 共同负责）
   - 积木的绘制（几何形状、颜色、圆角、边框）
   - 递归布局算法：自动排列主块和子块
   - 选中态、高亮态的视觉反馈
   - 动画效果：滑入动画、缓动函数

4. **Theme 主题系统**
   - 浅色/暗色主题的配色方案定义
   - 8 种语义颜色的转换
   - 全局主题切换时的组件更新
   - QSS 样式表的生成和应用

5. **用户体验优化**
   - 响应式布局
   - 流畅的动画过渡
   - 清晰的视觉反馈


---

#### 吴杰稀 - UI 组件与测试

**负责模块**：TestPanel + BlockPalette + BlockEditor + Util 工具

**具体职责**：
1. **TestPanel 测试面板**
   - 正则表达式显示框（QLineEdit）
   - 测试文本输入框（QPlainTextEdit）
   - 匹配结果列表（QListWidget）
   - 正则选项复选框（i/g/m/u/y/s）
   - 替换功能：替换字符串输入、替换全部按钮
   - 防抖处理：100ms 延迟匹配
   - 高频优化：拖动时的预览模式（不执行匹配）
   - 点击匹配项时的编辑器自动跳转

2. **BlockPalette 积木库**
   - 积木列表的分类显示（字面量、字符类、量词等）
   - 分组标题的视觉分离
   - 拖拽源的 MIME 数据编码（JSON Block 序列化）
   - 主题切换时的样式更新
   - 滚动和搜索辅助功能

3. **BlockEditor 积木编辑对话框**
   - 为不同 Block 类型提供编辑界面
   - 量词的范围编辑（如 {n,m}）
   - 字符集的内容编辑
   - 字面量的文本编辑
   - 分组类型（捕获 vs 非捕获）选择
   - 输入验证和错误提示

4. **Util 工具函数**
   - AppIcon：应用图标的生成和加载
   - Fonts：字体管理和跨平台适配
   - 颜色工具函数
   - 字符串转义/反转义工具

5. **集成测试与验收**
   - 功能测试：各个功能的自动化测试
   - 回归测试：新版本的兼容性测试
   - 性能测试：大规模积木的性能检查
   - 文档：使用手册和 API 文档


---

## 五、项目总结与反思

### 5.1 项目成就

#### 创新设计
- **Scratch 风格的正则表达式编辑器**：首次将积木拖拽的概念应用于正则表达式领域，大幅降低了学习门槛
- **双向编辑**：积木 ↔ 正则的实时同步，用户可灵活选择编辑方式
- **物理容器分组**：分组不再是抽象概念，而是可见的、可拖拽的容器

#### 技术完成度
- ✅ 完整的 Block 类体系，支持 8+ 种积木类型
- ✅ 双向转换：正则字符串 ↔ Block 树（编译 + 解析）
- ✅ 完整的撤销/重做系统（Qt QUndoStack）
- ✅ 高效的交互体验（拖动期间的高频优化）
- ✅ 7 个预置模板，开箱即用
- ✅ 跨平台兼容（macOS / Windows / Linux）

#### 用户体验
- 直观的拖拽交互，学习曲线平缓
- 实时反馈和丰富的动画效果
- 专业级的测试工具，支持 6 种正则选项和捕获组查看
- Material Design 配色，清爽的视觉设计

### 5.2 技术亮点

#### 1. **数据与渲染的彻底分离**
```
Block（纯数据）
  ↓
BlockGraphicsItem（渲染视图）
```
优势：易于测试、扩展性强、支持多种渲染方案（如导出为 SVG）

#### 2. **命令模式的优雅应用**
- 所有修改操作都是原子的 QUndoCommand
- 自动支持撤销/重做，无额外工作量
- 便于功能扩展和维护

#### 3. **嵌套分组的精确寻址**
```cpp
BlockPath path = {2, 0};  // 第 2 个分组的第 0 个子块
Block *block = canvas->blockAt(path);
```
支持任意深度的嵌套，通过列表索引精确定位

#### 4. **高频优化的双路径设计**
```cpp
// 拖动时：仅更新 UI，不执行匹配
setRegexPreview(regex);

// 停止拖动时：执行完整匹配
setRegex(regex);
runMatch();
```
实现 60Hz 的流畅交互，同时保证正确性

### 5.3 存在的不足与改进方向

#### 当前限制
1. **正则方言**：目前基于 Qt QRegularExpression（PCRE2），对其他语言的方言支持有限
2. **性能边界**：大规模积木（100+ 个）时的渲染性能有待优化
3. **辅助功能**：缺少屏幕阅读器支持，对视障用户不友好

#### 未来改进方向
1. **多方言支持**：
   - 检测用户选择的目标语言（Python/JavaScript/Java）
   - 在解析和编译阶段自动转换方言差异
   - 提示用户可能不兼容的积木组合

2. **性能优化**：
   - 虚拟滚动：只渲染可见的积木
   - 增量布局：只重新计算受影响的积木
   - 渲染缓存：复杂积木树的预渲染

3. **功能扩展**：
   - 积木库自定义：用户可自定义积木模板
   - 正则库分享：社区正则表达式库
   - 代码补全：在文本模式下支持代码补全和高亮

4. **协作编辑**：
   - 支持多人实时协作编辑
   - 云存储和版本控制
   - 分享链接预览


---

## 附录：项目构建与运行

### 系统要求
- **开发环境**：Qt 6.x、CMake 3.20+、Ninja
- **编译器**：GCC 11+ / Clang 13+ / MSVC 2019+
- **操作系统**：macOS 10.15+ / Windows 10+ / Ubuntu 20.04+

### 构建步骤

**macOS**：
```bash
brew install qt cmake ninja
./scripts/build-mac.sh
open ./build/RegexBlocks.app
```

**Windows**：
```cmd
scripts\build-windows.bat
build\RegexBlocks.exe
```

**Linux**：
```bash
sudo apt install qt6-base-dev cmake ninja-build
./scripts/build-linux.sh
./build/RegexBlocks
```

### 核心文件结构
```
src/
├── core/           # 业务逻辑（Block、Compiler、Parser、Commands）
├── ui/             # 用户界面（MainWindow、Canvas、Palette、TestPanel）
├── graphics/       # 图形渲染（BlockGraphicsItem）
└── util/           # 工具函数（AppIcon、Fonts、Theme）
```
